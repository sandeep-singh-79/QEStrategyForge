# Benchmarks

This folder stores deterministic benchmark scenarios for the strategy generator.

Each benchmark should contain:
- an input file
- expected assertions

The purpose is to make improvement measurable through pass/fail validation.
